---
type Props = {
    text: string
}
const {title, id} = Astro.props
const style = id ? `view-transition-name: ${id};` : null
---

<p class="text-2xl md:text-4xl font-bold mb-4 inline-block" style={style}>
    {title}
</p>
